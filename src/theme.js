import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    mode: 'light',
    primary: {
      main: '#003459',
    },
    secondary: {
      main: '#007EA7',
    },
    background: {
      default: '#f6f6f6',
    },
  },
});

export default theme;